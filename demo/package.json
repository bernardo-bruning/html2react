{
  "name": "html-to-react-bootstrap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "azure-deploy": "az storage blob upload-batch -s ./build -d $web",
    "deploy": "yarn build && yarn azure-deploy"
  },
  "dependencies": {
    "antd": "^3.22.2",
    "copy-to-clipboard": "^3.2.0",
    "draft-js": "^0.11.0",
    "html-to-react-parser": "^0.0.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.1.1",
    "react-syntax-highlighter": "^11.0.2"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
